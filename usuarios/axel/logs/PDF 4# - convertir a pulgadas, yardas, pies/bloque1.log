 
============================================================ 
INTENTO: 2026-05-02T23-51-54 
ARCHIVO: C:\Users\axelb\estudio-socratico\Ejercicios\PDF 4# - convertir a pulgadas, yardas, pies.c 
============================================================ 
[CODIGO FUENTE] 
------------------------------------------------------------ 
/*Haga un programa al cual se le digite una longitud expresada en
pulgadas e imprima la misma longitud en yardas, pies y
pulgadas.  Por ejemplo, una longitud de 65 pulgadas sería
expresada como 1 yarda, 2 pies y 5 pulgadas. */
#include <stdio.h>
int main()
{

    // lo primero es crear las variables donde vamos a almacenar los datos
    // luego pedirle al usurario cada dato y almacenarlos en sus respesctivas variables

    int pulgadas = 0;
    int yardas = 0;
    int pies = 0;

    printf("Digite la cantidad de pulgadas que quieres convertir: ");
    scanf("%d", &pulgadas);
    if (pulgadas == 0)
    {

        return 0;
    }

    // tengo que convertir ahora los datosd introducidos en pulgadas, a yardas y a pies. hagamoslo en orden
    // al dividir la cantidad de pulgadas que nos dieron, entre lo que vale 1 pie, obtenemos la cantidad de pies
    // que representa esa cantidad de pulgadas
    // lo mismo pasa si lo hacemos con las yardas y los pies;

    // ahora el programa dice que tiene que ser, la misma longitud, pero expresada en todas las unidades de medida
    // para ello necesito quedarme con los valores que sobran de cada division.

    // primero sacamos el residuo y luego dividimos
    // al reves entonces

    // lo hacemos primero con las yardas
    yardas = pulgadas / 36;
    pulgadas %= 36;

    pies = pulgadas / 12;
    pulgadas %= 12;

    // hay que imprimir la cantidad final
    printf("%d Yardas, %d pies y %d pulgadas\n", yardas, pies, pulgadas);

    return 0;
}
 
------------------------------------------------------------ 
[OUTPUT DEL COMPILADOR] 
(Compilacion limpia. Cero errores y advertencias.) 
[EXIT CODE: 0] 
 
============================================================ 
INTENTO: 2026-05-05T04-34-52 
ARCHIVO: C:\Users\AxelBlade\.gemini\antigravity\scratch\estudio-socratico\Ejercicios\PDF 4# - convertir a pulgadas, yardas, pies.c 
============================================================ 
[CODIGO FUENTE] 
------------------------------------------------------------ 
/*Haga un programa al cual se le digite una longitud expresada en
pulgadas e imprima la misma longitud en yardas, pies y
pulgadas.  Por ejemplo, una longitud de 65 pulgadas sería
expresada como 1 yarda, 2 pies y 5 pulgadas. */
#include <stdio.h>
int main()
{

    // lo primero es crear las variables donde vamos a almacenar los datos
    // luego pedirle al usurario cada dato y almacenarlos en sus respesctivas variables

    int pulgadas = 0;
    int yardas = 0;
    int pies = 0;

    printf("Digite la cantidad de pulgadas que quieres convertir: ");
    scanf("%d", &pulgadas);
    if (pulgadas == 0)
    {

        return 0;
    }

    // tengo que convertir ahora los datosd introducidos en pulgadas, a yardas y a pies. hagamoslo en orden
    // al dividir la cantidad de pulgadas que nos dieron, entre lo que vale 1 pie, obtenemos la cantidad de pies
    // que representa esa cantidad de pulgadas
    // lo mismo pasa si lo hacemos con las yardas y los pies;

    // ahora el programa dice que tiene que ser, la misma longitud, pero expresada en todas las unidades de medida
    // para ello necesito quedarme con los valores que sobran de cada division.

    // primero sacamos el residuo y luego dividimos
    // al reves entonces

    // lo hacemos primero con las yardas
    yardas = pulgadas / 36;
    pulgadas %= 36;

    pies = pulgadas / 12;
    pulgadas %= 12;

    // hay que imprimir la cantidad final
    printf("%d Yardas, %d pies y %d pulgadas\n", yardas, pies, pulgadas);

    return 0;
}
 
------------------------------------------------------------ 
[OUTPUT DEL COMPILADOR] 
C:/msys64/mingw64/bin/../lib/gcc/x86_64-w64-mingw32/14.2.0/../../../../x86_64-w64-mingw32/bin/ld.exe: cannot open output file C:\Users\AxelBlade\.gemini\antigravity\scratch\estudio-socratico\_output.exe: Permission denied
collect2.exe: error: ld returned 1 exit status
[EXIT CODE: 1] 
 
============================================================ 
INTENTO: 2026-05-05T04-37-45 
ARCHIVO: C:\Users\AxelBlade\.gemini\antigravity\scratch\estudio-socratico\Ejercicios\PDF 4# - convertir a pulgadas, yardas, pies.c 
============================================================ 
[CODIGO FUENTE] 
------------------------------------------------------------ 
/*Haga un programa al cual se le digite una longitud expresada en
pulgadas e imprima la misma longitud en yardas, pies y
pulgadas.  Por ejemplo, una longitud de 65 pulgadas sería
expresada como 1 yarda, 2 pies y 5 pulgadas. */
#include <stdio.h>
int main()
{

    // lo primero es crear las variables donde vamos a almacenar los datos
    // luego pedirle al usurario cada dato y almacenarlos en sus respesctivas variables

    int pulgadas = 0;
    int yardas = 0;
    int pies = 0;

    printf("Digite la cantidad de pulgadas que quieres convertir: ");
    scanf("%d", &pulgadas);
    if (pulgadas == 0)
    {

        return 0;
    }

    // tengo que convertir ahora los datosd introducidos en pulgadas, a yardas y a pies. hagamoslo en orden
    // al dividir la cantidad de pulgadas que nos dieron, entre lo que vale 1 pie, obtenemos la cantidad de pies
    // que representa esa cantidad de pulgadas
    // lo mismo pasa si lo hacemos con las yardas y los pies;

    // ahora el programa dice que tiene que ser, la misma longitud, pero expresada en todas las unidades de medida
    // para ello necesito quedarme con los valores que sobran de cada division.

    // primero sacamos el residuo y luego dividimos
    // al reves entonces

    // lo hacemos primero con las yardas
    yardas = pulgadas / 36;
    pulgadas %= 36;

    pies = pulgadas / 12;
    pulgadas %= 12;

    // hay que imprimir la cantidad final
    printf("%d Yardas, %d pies y %d pulgadas\n", yardas, pies, pulgadas);

    return 0;
}
 
------------------------------------------------------------ 
[OUTPUT DEL COMPILADOR] 
(Compilacion limpia. Cero errores y advertencias.) 
[EXIT CODE: 0] 

[EJECUCION DEL USUARIO]
Digite la cantidad de pulgadas que quieres convertir: e *  Executing task: .\build.cmd 'C:\Users\AxelBlade\.gemini\antig
ravity\scratch\estudio-socratico\Ejercicios\PDF 4# - convertir a pulgadas, yardas, pies.c'

================================
 Programa finalizado.

------------------------------------------------------------
 
============================================================ 
INTENTO: 2026-05-05T04-50-11 
ARCHIVO: C:\Users\AxelBlade\.gemini\antigravity\scratch\estudio-socratico\Ejercicios\PDF 4# - convertir a pulgadas, yardas, pies.c 
============================================================ 
[CODIGO FUENTE] 
------------------------------------------------------------ 
/*Haga un programa al cual se le digite una longitud expresada en
pulgadas e imprima la misma longitud en yardas, pies y
pulgadas.  Por ejemplo, una longitud de 65 pulgadas sería
expresada como 1 yarda, 2 pies y 5 pulgadas. */
#include <stdio.h>
int main()
{

    // lo primero es crear las variables donde vamos a almacenar los datos
    // luego pedirle al usurario cada dato y almacenarlos en sus respesctivas variables

    int pulgadas = 0;
    int yardas = 0;
    int pies = 0;

    printf("Digite la cantidad de pulgadas que quieres convertir: ");
    scanf("%d", &pulgadas);
    if (pulgadas == 0)
    {

        return 0;
    }

    // tengo que convertir ahora los datosd introducidos en pulgadas, a yardas y a pies. hagamoslo en orden
    // al dividir la cantidad de pulgadas que nos dieron, entre lo que vale 1 pie, obtenemos la cantidad de pies
    // que representa esa cantidad de pulgadas
    // lo mismo pasa si lo hacemos con las yardas y los pies;

    // ahora el programa dice que tiene que ser, la misma longitud, pero expresada en todas las unidades de medida
    // para ello necesito quedarme con los valores que sobran de cada division.

    // primero sacamos el residuo y luego dividimos
    // al reves entonces

    // lo hacemos primero con las yardas
    yardas = pulgadas / 36;
    pulgadas %= 36;

    pies = pulgadas / 12;
    pulgadas %= 12;

    // hay que imprimir la cantidad final
    printf("%d Yardas, %d pies y %d pulgadas\n", yardas, pies, pulgadas);

    return 0;
}
 
------------------------------------------------------------ 
[OUTPUT DEL COMPILADOR] 
(Compilacion limpia. Cero errores y advertencias.) 
[EXIT CODE: 0] 
 
============================================================ 
INTENTO: 2026-05-05T04-52-22 
ARCHIVO: C:\Users\AxelBlade\.gemini\antigravity\scratch\estudio-socratico\Ejercicios\PDF 4# - convertir a pulgadas, yardas, pies.c 
============================================================ 
[CODIGO FUENTE] 
------------------------------------------------------------ 
/*Haga un programa al cual se le digite una longitud expresada en
pulgadas e imprima la misma longitud en yardas, pies y
pulgadas.  Por ejemplo, una longitud de 65 pulgadas sería
expresada como 1 yarda, 2 pies y 5 pulgadas. */
#include <stdio.h>
int main()
{

    // lo primero es crear las variables donde vamos a almacenar los datos
    // luego pedirle al usurario cada dato y almacenarlos en sus respesctivas variables

    int pulgadas = 0;
    int yardas = 0;
    int pies = 0;

    printf("Digite la cantidad de pulgadas que quieres convertir: ");
    scanf("%d", &pulgadas);
    if (pulgadas == 0)
    {

        return 0;
    }

    // tengo que convertir ahora los datosd introducidos en pulgadas, a yardas y a pies. hagamoslo en orden
    // al dividir la cantidad de pulgadas que nos dieron, entre lo que vale 1 pie, obtenemos la cantidad de pies
    // que representa esa cantidad de pulgadas
    // lo mismo pasa si lo hacemos con las yardas y los pies;

    // ahora el programa dice que tiene que ser, la misma longitud, pero expresada en todas las unidades de medida
    // para ello necesito quedarme con los valores que sobran de cada division.

    // primero sacamos el residuo y luego dividimos
    // al reves entonces

    // lo hacemos primero con las yardas
    yardas = pulgadas / 36;
    pulgadas %= 36;

    pies = pulgadas / 12;
    pulgadas %= 12;

    // hay que imprimir la cantidad final
    printf("%d Yardas, %d pies y %d pulgadas\n", yardas, pies, pulgadas);

    return 0;
}
 
------------------------------------------------------------ 
[OUTPUT DEL COMPILADOR] 
(Compilacion limpia. Cero errores y advertencias.) 
[EXIT CODE: 0] 
 
============================================================ 
INTENTO: 2026-05-05T04-55-46 
ARCHIVO: C:\Users\AxelBlade\.gemini\antigravity\scratch\estudio-socratico\Ejercicios\PDF 4# - convertir a pulgadas, yardas, pies.c 
============================================================ 
[CODIGO FUENTE] 
------------------------------------------------------------ 
/*Haga un programa al cual se le digite una longitud expresada en
pulgadas e imprima la misma longitud en yardas, pies y
pulgadas.  Por ejemplo, una longitud de 65 pulgadas sería
expresada como 1 yarda, 2 pies y 5 pulgadas. */
#include <stdio.h>
int main()
{

    // lo primero es crear las variables donde vamos a almacenar los datos
    // luego pedirle al usurario cada dato y almacenarlos en sus respesctivas variables

    int pulgadas = 0;
    int yardas = 0;
    int pies = 0;

    printf("Digite la cantidad de pulgadas que quieres convertir: ");
    scanf("%d", &pulgadas);
    if (pulgadas == 0)
    {

        return 0;
    }

    // tengo que convertir ahora los datosd introducidos en pulgadas, a yardas y a pies. hagamoslo en orden
    // al dividir la cantidad de pulgadas que nos dieron, entre lo que vale 1 pie, obtenemos la cantidad de pies
    // que representa esa cantidad de pulgadas
    // lo mismo pasa si lo hacemos con las yardas y los pies;

    // ahora el programa dice que tiene que ser, la misma longitud, pero expresada en todas las unidades de medida
    // para ello necesito quedarme con los valores que sobran de cada division.

    // primero sacamos el residuo y luego dividimos
    // al reves entonces

    // lo hacemos primero con las yardas
    yardas = pulgadas / 36;
    pulgadas %= 36;

    pies = pulgadas / 12;
    pulgadas %= 12;

    // hay que imprimir la cantidad final
    printf("%d Yardas, %d pies y %d pulgadas\n", yardas, pies, pulgadas);
        printf("%c\n", 3);


    return 0;
}
 
------------------------------------------------------------ 
[OUTPUT DEL COMPILADOR] 
(Compilacion limpia. Cero errores y advertencias.) 
[EXIT CODE: 0] 

[EJECUCION DEL USUARIO]
Digite la cantidad de pulgadas que quieres convertir: 3
0 Yardas, 0 pies y 3 pulgadas


================================
 Programa finalizado.

------------------------------------------------------------
 
============================================================ 
INTENTO: 2026-05-05T04-57-32 
ARCHIVO: C:\Users\AxelBlade\.gemini\antigravity\scratch\estudio-socratico\Ejercicios\PDF 4# - convertir a pulgadas, yardas, pies.c 
============================================================ 
[CODIGO FUENTE] 
------------------------------------------------------------ 
/*Haga un programa al cual se le digite una longitud expresada en
pulgadas e imprima la misma longitud en yardas, pies y
pulgadas.  Por ejemplo, una longitud de 65 pulgadas sería
expresada como 1 yarda, 2 pies y 5 pulgadas. */
#include <stdio.h>
int main()
{

    // lo primero es crear las variables donde vamos a almacenar los datos
    // luego pedirle al usurario cada dato y almacenarlos en sus respesctivas variables

    int pulgadas = 0;
    int yardas = 0;
    int pies = 0;

    printf("Digite la cantidad de pulgadas que quieres convertir: ");
    scanf("%d", &pulgadas);
    if (pulgadas == 0)
    {

        return 0;
    }

    // tengo que convertir ahora los datosd introducidos en pulgadas, a yardas y a pies. hagamoslo en orden
    // al dividir la cantidad de pulgadas que nos dieron, entre lo que vale 1 pie, obtenemos la cantidad de pies
    // que representa esa cantidad de pulgadas
    // lo mismo pasa si lo hacemos con las yardas y los pies;

    // ahora el programa dice que tiene que ser, la misma longitud, pero expresada en todas las unidades de medida
    // para ello necesito quedarme con los valores que sobran de cada division.

    // primero sacamos el residuo y luego dividimos
    // al reves entonces

    // lo hacemos primero con las yardas
    yardas = pulgadas / 36;
    pulgadas %= 36;

    pies = pulgadas / 12;
    pulgadas %= 12;

    // hay que imprimir la cantidad final
    printf("%d Yardas, %d pies y %d pulgadas\n", yardas, pies, pulgadas);
        printf("%c\n", 3);


    return 0;
}
 
------------------------------------------------------------ 
[OUTPUT DEL COMPILADOR] 
(Compilacion limpia. Cero errores y advertencias.) 
[EXIT CODE: 0] 

[EJECUCION DEL USUARIO]
Digite la cantidad de pulgadas que quieres convertir: 65
1 Yardas, 2 pies y 5 pulgadas


================================
 Programa finalizado.

------------------------------------------------------------
 
============================================================ 
INTENTO: 2026-05-05T04-58-19 
ARCHIVO: C:\Users\AxelBlade\.gemini\antigravity\scratch\estudio-socratico\Ejercicios\PDF 4# - convertir a pulgadas, yardas, pies.c 
============================================================ 
[CODIGO FUENTE] 
------------------------------------------------------------ 
/*Haga un programa al cual se le digite una longitud expresada en
pulgadas e imprima la misma longitud en yardas, pies y
pulgadas.  Por ejemplo, una longitud de 65 pulgadas sería
expresada como 1 yarda, 2 pies y 5 pulgadas. */
#include <stdio.h>
int main()
{

    // lo primero es crear las variables donde vamos a almacenar los datos
    // luego pedirle al usurario cada dato y almacenarlos en sus respesctivas variables

    int pulgadas = 0;
    int yardas = 0;
    int pies = 0;

    printf("Digite la cantidad de pulgadas que quieres convertir: ");
    scanf("%d", &pulgadas);
    if (pulgadas == 0)
    {

        return 0;
    }

    // tengo que convertir ahora los datosd introducidos en pulgadas, a yardas y a pies. hagamoslo en orden
    // al dividir la cantidad de pulgadas que nos dieron, entre lo que vale 1 pie, obtenemos la cantidad de pies
    // que representa esa cantidad de pulgadas
    // lo mismo pasa si lo hacemos con las yardas y los pies;

    // ahora el programa dice que tiene que ser, la misma longitud, pero expresada en todas las unidades de medida
    // para ello necesito quedarme con los valores que sobran de cada division.

    // primero sacamos el residuo y luego dividimos
    // al reves entonces

    // lo hacemos primero con las yardas
    yardas = pulgadas / 36;
    pulgadas %= 36;

    pies = pulgadas / 12;
    pulgadas %= 12;

    // hay que imprimir la cantidad final
    //printf("%d Yardas, %d pies y %d pulgadas\n", yardas, pies, pulgadas);
        printf("%c\n", 3);


    return 0;
}
 
------------------------------------------------------------ 
[OUTPUT DEL COMPILADOR] 
PDF 4# - convertir a pulgadas, yardas, pies.c: In function 'main':
PDF 4# - convertir a pulgadas, yardas, pies.c:14:9: warning: variable 'pies' set but not used [-Wunused-but-set-variable]
   14 |     int pies = 0;
      |         ^~~~
PDF 4# - convertir a pulgadas, yardas, pies.c:13:9: warning: variable 'yardas' set but not used [-Wunused-but-set-variable]
   13 |     int yardas = 0;
      |         ^~~~~~
[EXIT CODE: 0] 

[EJECUCION DEL USUARIO]
Digite la cantidad de pulgadas que quieres convertir: 65


================================
 Programa finalizado.

------------------------------------------------------------
